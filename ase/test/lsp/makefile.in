SRCS = lsp.c
OUTS = $(SRCS:.c=.x)

CC = @CC@
CFLAGS = @CFLAGS@ -I@abs_top_builddir@/..
LDFLAGS = @LDFLAGS@ -L@abs_top_builddir@/lsp
LIBS = @LIBS@ -laselsp

all: $(OUTS)

clean:
	rm -rf $(OUTS) *.o

.SUFFIXES: .c .x
.c.x:
	$(CC) $(CFLAGS) $(LDFLAGS) -o $@ $< $(LIBS)
